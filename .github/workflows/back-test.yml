name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install npm dependencies
      run: npm install
      working-directory: ./tasky-back
    - name: run npm tests
      run: npm run test
      working-directory: ./tasky-back
